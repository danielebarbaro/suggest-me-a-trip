        <div class="header">
            <div class="header-top">
                <div class="header-content">
                    <h1>{{ title }}</h1>
                    <div class="subtitle">{{ date }}</div>
                </div>
                <div class="logo-area">🚌</div>
            </div>
        </div> 
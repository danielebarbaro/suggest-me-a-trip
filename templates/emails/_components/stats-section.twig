            <div class="stats-section">
                <div class="stats">
                    {% for stat in stats %}
                        <div class="stat-card">
                            <div class="stat-number">{{ stat.number }}</div>
                            <div class="stat-label">{{ stat.label }}</div>
                        </div>
                    {% endfor %}
                </div>
            </div> 